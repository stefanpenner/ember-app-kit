module.exports = {
  'debug': ['tmp'],
  'dist': ['tmp', 'dist'],
  // warning this can be destructive to your project source
  // make sure you have importants changes in version control
  // before running clean task
  'app': ['app']
};
